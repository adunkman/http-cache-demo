{{#if status}}
  <div class="headers">
    <h1>{{status}}</h1>
    <table>
      {{#each headers}}
        <tr class="{{key}}">
          <th>{{key}}:</th>
          <td>{{value}}</td>
        </tr>
      {{/each}}
    </table>
  </div>
{{/if}}
